#include <bits/stdc++.h>
using namespace std;

int main() {
  ios::sync_with_stdio(0);
  cin.tie(0);

  int t;
  cin >> t;
  while (t--) {
    int n, b;
    cin >> n >> b;

    vector<int> a(n);
    for (int i = 0; i < n; ++i) {
      cin >> a[i];
    }
  }

  return 0;
}
